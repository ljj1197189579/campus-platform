# 测试报告

测试日期：2026-09-11。本文替代此前仅验证演示模式的报告，所有“通过”均限定于下列实际检查范围。

## 环境与构建

JDK 17.0.12，Maven 3.9.15，MySQL 8.0.42，Vue 3，Vite 8.2.2。
前端 npm run build 成功；后端 mvn verify 成功。前端仍有大于500KB的包体积警告，尚未完成按页面拆包优化。

## 自动化结果

本次执行 backend/test-mysql.ps1：25项测试，失败0，错误0，跳过0。

| 测试类 | 数量 | 验证范围 |
|---|---:|---|
| JwtAuthorizationTest | 8 | 缺少、伪造、过期令牌；账号禁用；角色变化；数据库故障；正常用户与公开读取 |
| OrderSecurityTest | 11 | 买卖双方权限；第三方越权；接单与库存预留；取消释放；收货；重复操作；评价权限及范围 |
| ProductionRegressionTest | 5 | 数据库故障不回退演示登录；密码哈希；空参数；配送及价格校验；重复审核 |
| MySqlFlowTest | 1条完整流程 | 真实MySQL上注册登录、图片上传与所有权、商品与失物审核及搜索、接单、私密付款凭证、人工核实、确认收货、评价、取消及退款记录 |

该集成流程还验证：伪装图片拒绝、普通用户不能设置收款码或核实付款、卖家不能代替买家提交凭证、未核实到账不可确认收货、实际金额不符拒绝、付款与到账单号重复拒绝、重复确认拒绝、已确认到账不能标记为未收到。12.50元订单服务费为0.25元。测试收款配置仅存在于隔离数据库，没有发生真实付款。

前端 npm test：7项测试全部通过，使用Vitest、Vue Test Utils和jsdom，覆盖底部导航与中文类型状态、多图上传和无图校验、失物搜索、模拟计时器推进后自动更新商品、未核实付款禁止确认收货、100元商品只展示2元服务费以及提交凭证待核实。

以上是25项后端测试（含1条完整MySQL流程）及7项前端组件测试，不是25个独立集成场景，也不是浏览器端到端测试。

MySqlFlowTest 使用真实 MySQL 和 Spring MockMvc 经过控制器、过滤器与事务访问层。它不是浏览器测试，也不是对正式库进行批量写入测试。

隔离数据库仅监听127.0.0.1:33317，位于Public Documents下随机新建目录。先构造缺少配送字段与失物审核字段的旧版表结构，然后连续执行迁移两次。两次迁移均通过，原数据保留。验证结束后测试实例已停止。

## 正式本机数据库与HTTP联调

用户配置的3306数据库连接已成功。配送等迁移前备份为 backend/backups/before-delivery-reviews-20260911-121106.sql；图片与服务费迁移前另备份到 backend/backups/before-media-fees-20260911-132639.sql。新迁移已经应用。

发现并修复：旧版 lost_found 缺少 audit_status，导致公开失物接口返回503。新增兼容迁移后接口恢复。数据库原先无用户，已初始化随机密码管理员；没有覆盖任何已有用户。

在实际运行的后端8080端口验证：

| 场景 | HTTP结果 |
|---|---:|
| 管理员使用新生成的本机账号登录 | 200 |
| GET /api/goods | 200 |
| GET /api/lost-found | 200 |
| 未登录 GET /api/orders | 401 |
| 登录后 GET /api/orders | 200 |
| 登录后 GET /api/me/credit | 200 |
| 管理员 GET /api/admin/audits | 200 |
| 登录后 GET /api/wallet | 200 |
| GET /api/payment-settings | 200 |
| 管理员 GET /api/admin/payments | 200 |
| 管理员 GET /api/admin/revenue | 200 |

在前端5173端口验证首页、App.vue、OrderCenter.vue模块返回200；通过Vite代理访问 /api/goods 和 /api/lost-found 均返回200。此前已检查只读预览首页的分类、商品、轮播与底部导航可访问内容；未将其算作完整浏览器端到端验收。

本次更新后重新启动后端，并验证5173代理的 /api/payment-settings 返回200。运行包与通过测试的构建产物SHA256一致。正式本机尚未配置真实收款码，需管理员上传后才能使用收款流程；没有将测试收款码写入正式库。

## 已知限制

- 三张AI轮播图调用官方API均返回401 invalid_api_key；没有生成图片，不能标为完成。
- 未完成完整浏览器交互测试、移动设备截图验收、并发压测及公网部署验证。
- 商品搜索仍在前端对最多200条商品筛选，尚非数据库分页检索；失物搜索已使用后端关键词查询。
- 自动更新采用约5秒轮询，网络延迟与后台标签页调度会影响实际更新间隔，不是即时推送。
- 个人收款码无法自动验证实际到账；测试验证的是权限与人工核实状态流转，没有验证真实支付机构到账或退款。
- MyBatis-Plus、Vue Router及部分生产功能尚未落地，详见README。
- 测试通过不表示所有未来场景均无bug，也不等同于完整生产验收。
